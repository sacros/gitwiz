# gitwiz - utility to manage github repositories from cli

### How to use:

- Create a new repo "repo_name"
    ```sh
    ./gitwiz.sh create repo_name
    ```
- Delete an existing repo "repo_name"
    ```sh
    ./gitwiz.sh delete repo_name
    ```
